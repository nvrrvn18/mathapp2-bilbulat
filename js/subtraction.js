function renderSubtraction(){setLast('subtraction');let pairs=0;app.innerHTML=lessonShell('Pengurangan Bilangan Bulat','Mengurangi berarti mengambil kartu.',`<div class="section"><p class="eyebrow">Masalah</p><h2>2 − (−3)</h2><p>Kita memiliki dua kartu positif, tetapi harus mengambil tiga kartu negatif. Kartu negatif belum tersedia.</p><div id="subCards" class="cards">${cards(2,0)}</div><button id="zeroBtn" class="btn secondary">+ Tambahkan pasangan nol</button><p class="small">Menambahkan +1 dan −1 tidak mengubah nilai karena jumlahnya 0.</p><button id="takeBtn" class="btn" disabled>Ambil 3 kartu negatif</button><div id="subFeedback"></div></div>`);zeroBtn.onclick=()=>{if(pairs<3){pairs++;subCards.insertAdjacentHTML('beforeend',card(1,'zp'+pairs)+card(-1,'zn'+pairs));if(pairs===3)takeBtn.disabled=false}};takeBtn.onclick=()=>{[...subCards.querySelectorAll('.neg')].forEach(c=>c.classList.add('paired'));subFeedback.innerHTML='<div class="feedback">Tersisa 5 kartu positif. Jadi <b>2 − (−3) = 5</b>.</div><div class="remember">Yang perlu diingat: pasangan nol dapat ditambahkan tanpa mengubah nilai awal.</div>';complete('subtraction');nextButton('multiplication')}}
