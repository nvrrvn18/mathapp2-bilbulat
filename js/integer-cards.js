function card(v,i=''){return `<button class="int-card ${v>0?'pos':'neg'}" data-v="${v}" data-i="${i}" aria-label="kartu ${v>0?'positif':'negatif'} satu">${v>0?'+1':'−1'}</button>`}function cards(pos,neg){return Array.from({length:pos},(_,i)=>card(1,'p'+i)).join('')+Array.from({length:neg},(_,i)=>card(-1,'n'+i)).join('')}function pairGame(container,onDone){let selected=null;container.querySelectorAll('.int-card').forEach(c=>c.onclick=()=>{if(c.classList.contains('paired'))return;if(!selected){selected=c;c.classList.add('selected');return}if(selected===c){c.classList.remove('selected');selected=null;return}if(+selected.dataset.v + +c.dataset.v===0){selected.classList.add('paired');c.classList.add('paired');selected.classList.remove('selected');selected=null;onDone?.()}else{c.classList.add('shake');setTimeout(()=>c.classList.remove('shake'),300);selected.classList.remove('selected');selected=null}})}function choices(opts,answer,cb){return `<div class="choice-grid">${opts.map(x=>`<button class="choice" data-a="${x}">${x>0?'+'+x:x}</button>`).join('')}</div><div id="feedback"></div>`}
